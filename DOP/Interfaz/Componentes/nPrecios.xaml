<UserControl x:Class="DataObra.Interfaz.Componentes.nPrecios"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="10,10,10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="pack://application:,,,/Imagenes/Material.png" Width="28" Height="28" Margin="0,0,8,0"/>
                <TextBlock Text="Precios"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="#43A047"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Contenido principal aquí -->
        <StackPanel Grid.Row="1">
            <!-- Agrega aquí el contenido principal de nPrecios -->
        </StackPanel>

        <Button
            Grid.Row="2"
            Width="200"
            Height="20"
            Click="Button_Click"
            Content="Ver Precios" />
    </Grid>
</UserControl>
